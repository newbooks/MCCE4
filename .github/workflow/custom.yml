name: Move assigned card
on:
  issues:
    types:
      - assigned
jobs:
  move-assigned-card:
    runs-on: ubuntu-latest
    steps:
      - uses: uses: alex-page/github-project-automation-plus@v0.8.3
        with:
          project: MCCE4
          column: In Progress
          repo-token: ${{ secrets.GITHUB_TOKEN }}
